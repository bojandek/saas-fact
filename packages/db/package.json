{
  "name": "@saas-factory/db",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "workspace:*",
    "@testing-library/react": "workspace:*",
    "jsdom": "workspace:*"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.4"
  }
}
